# will throw an error function has 3 params with 2nd default and
# is only passed one arg, so 3rd param is left empty also
# every subsequent param after first with default value must
# also contain a default value