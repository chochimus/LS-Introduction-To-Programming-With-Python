If a condition block ends in a return simply use
multiple ifs rather than unecessary if else.

along with short-circuiting of and and or operations
logical operators also return the last evaluated value